var mishtha = new Promise(function(genius,pagal) {
    const m = "mishtha";
    const d = "mishtha";
    if(m === d) { 
        genius(); 
      } else { 
        pagal(); 
      } 
});
mishtha. 
    then(function () { 
        console.log('mishtha can solve'); 
    }). 
    catch(function () { 
        console.log('dev  cant'); 
    }); 